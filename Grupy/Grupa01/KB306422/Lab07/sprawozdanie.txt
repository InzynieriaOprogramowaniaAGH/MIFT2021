1.
Umieszczenie plików źródłowych w podanym w instrukcji porządku folderów.

2.
Wykonano fork repo. Utworzono Jenkinsfile'a i umieszczono go w repo (po forku) node-chat-app. Następnie zaimplementowano Pipeline, dzięki czemu Jenkinsfile odpowiada aktualnie za testowanie aplikacji oraz wysyłanie powiadomień e-mail o sukcesie lub niepowodzeniu (build i build log). Żeby Jenkins mógł wykonać operację wysyłania maila wykorzystano plugin emailext oraz skonfigurowano połączenie SMTP gmail w ustawieniach Jenkis. Dodatkowo pobrano również na Jenkinsa nodeJS i skonfigurowano go dla poprawnego uruchomienia testów.

3.
Po skofigurowaniu nowego projektu i uruchomieniu pipeline w Jenkins początkowo testy się nie powiodły i została otrzymana taka infomacja, a następnie testy przeszły z sukcesem oraz został wysłany tym razem mail o powodzeniu razem z build logiem. Zrzuty ekranu umieszczono jako PNG. 

4.
Dodano Jenkinsfile zgodnie z porządkiem folderów z punktu 1 do KB306422 oraz do folderu Lab07.
