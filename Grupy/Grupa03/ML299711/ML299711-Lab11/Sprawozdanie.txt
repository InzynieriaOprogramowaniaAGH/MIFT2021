1. Utowrzyłem nowy branch prywatny.
2. Obraz dockerowy z budowaniem aplikacji (nowy dockerfile) wypchnołem na dockerhuba.
3. Zainstalowałem minikube i pobrałem kubectl.
	-minikube start nie chciało się uruchomić więc komendą <minikube config set driver docker> ustawiłem sterownik
	-następnie dodałem uprawnienia do dockera użytkownikowi <sudo usermod -aG docker $USER && newgrp docker>
4. Uruchoiłem dashboard. Odpowiada on za:
     	-wdrażanie aplikacji kontenerowych w klastrze Kubernetes
     	-rozwiązywanie problemów z aplikacją w kontenerze
     	-zarządzanie zasobami klastra
     	-uzyskiwanie przeglądu aplikacji działających w klastrze
     	-tworzenie lub modyfikowanie poszczególnych zasobów Kubernetes
5. Poleceniem <kubectl run socket-io --image=lesniakmateusz/lab11_run --port=9999 --labels app=Socket.io> uruchomiłem kontener następnie przekierowałem porty
6.  Klaster wyrzuca błąd związany z NPM i zmienną $PATH

