version: '3.3'
services:
    build-agent:
        container_name: docker_build
        image: docker_build:docker-build
        build:
            context: .
            dockerfile: Dockerfile-build
    test-agent:
        container_name: docker_test
        image: docker_test:docker_test
        build:
            context: .
            dockerfile: Dockerfile-test
        depends_on: 
        - build-agent
