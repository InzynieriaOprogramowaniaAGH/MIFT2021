Sprawozdanie nr 11

1. Upewnienie się, że zbudowany został poprawnie obraz dockerowy wraz ze zbudowanym komunikatorem.

2.Zainstalowanie klaster Kubernetes
--> curl -LO https://storage.googleapis.com/minikube/releases/latest/minikube-linux-amd64
--> sudo install minikube-linux-amd64 /usr/local/bin/minikube
--> minikube start --driver=docker

3. Włączenie i uruchomienie MiniKube Dashboard 
--> minikube dashboard

3.1 Możliwości korzystania z MiniKube Dashboard:
--> zarządzanie zasobami klastra
--> rozwiązywanie probelmów z aplikacją w kontenerze 
--> tworzenie lub modyfikacja zasobów Kubernetes

4. Uruchomienie kontenera-komunikatora w  Kubernetes
-->kubectl run komunikator --image=juliamatula/deltachat --port=9999 --labels app=komunikator

4.1 Sprawdzenie poprawności poprzez dokoanie przekierowania portów, w celu umożliwienia dostępu z zewnątrz 
--> kubectl port-forward deploy/komunikator 9999:3000

5. Udokukemntowanie działającego komunikatora w klastrze Kubernetes 